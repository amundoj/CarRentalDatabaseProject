<!DOCTYPE html>
<html>
<head>
  <title>Rented Vehicles</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/stylesheets/styles.css">
</head>
<body>
  <%- include('./partials/navbar.ejs', {user: user}) %>
  <div class="container-fluid mt-5">
    <h2 class="p-3 text-center">Rented Vehicles</h2>
    <div class="list-group">
      <div class="row px-3 py-1 w-100">
        <span class="col py-1 bg-noroff">Registration No.</span>
        <span class="col py-1 bg-noroff">Make</span>
        <span class="col py-1 bg-noroff">Model</span>
        <span class="col py-1 bg-noroff">Colour</span>
        <span class="col py-1 bg-noroff">Vehicle Type</span>
        <span class="col py-1 bg-noroff">Features</span>
        <span class="col py-1 bg-noroff">Rental Date</span>
      </div>
      <% rentals.forEach(function(rental) { %>
        <div class="row px-3 py-1 w-100">
          <span class="col py-1 bg-light"><%= rental.Vehicle.registrationNo %></span>
          <span class="col py-1 bg-light"><%= rental.Vehicle.make %></span>
          <span class="col py-1 bg-light"><%= rental.Vehicle.model %></span>
          <span class="col py-1 bg-light"><%= rental.Vehicle.colour %></span>
          <span class="col py-1 bg-light"><%= rental.Vehicle.vehicleType %></span>
          <span class="col py-1 bg-light"><%= rental.Vehicle.features %></span>
          <span class="col py-1 bg-light"><%= rental.rentalDate.toDateString() %></span>
        </div>
      <% }); %>
    </div>
  </div>
</body>
</html>
